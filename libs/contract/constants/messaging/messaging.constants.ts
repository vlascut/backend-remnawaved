export enum MessagingChannels {
    EVENT = 'redis-event',
}

export enum MessagingBuses {
    EVENT = 'event.bus',
}

export enum MessagingQueues {
    EVENT = 'event-queue',
}

export enum MessagingMessages {
    NODE_METRICS = 'node.metrics',
}
